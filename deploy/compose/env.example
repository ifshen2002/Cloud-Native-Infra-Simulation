# Registry and tag for images
REGISTRY=local
TAG=dev

# App metadata
APP_VERSION=0.1.0
GIT_SHA=dev

# Hostnames for routing
API_HOST=api.localhost
TRAEFIK_HOST=traefik.localhost

# Postgres defaults (DO NOT USE IN PROD)
POSTGRES_DB=app
POSTGRES_USER=app
POSTGRES_PASSWORD=app


